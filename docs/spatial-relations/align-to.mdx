---
sidebar_position: 4
title: "Align To"
slug: "align-to"
---

# Align To

The `align_to` function is used to align a mobject to a specific edge of another mobject or point.

#### Parameters

- `point_or_mobject` (`Point3D | Mobject`): The point or mobject to align to.
- `edge` (`Vector3`, default=`UP`): The edge of this mobject to align with the target. Must be one of `UP`, `DOWN`, `LEFT`, `RIGHT`, `ORIGIN`.
- `buff` (`float`, default=`0`): The buffer distance to apply after alignment.

#### Example 1

A stretched circle aligned to the top edge of a rectangle, with a tiny buff.

```python
from smanim import *

rect = Rectangle(width=1, height=2, color=GRAY)
circle = Circle(radius=0.5, color=DARK_GRAY, z_index=2)
circle.stretch(1.5, dim=0)
circle.align_to(rect, UP, buff=TINY_BUFF)

canvas.add(rect, circle)
canvas.draw(crop=True, ignore_bg=True)
```

<img
  src="/img/spatial-relations/align-to/ex1.svg"
  alt="Align to Example 1"
  style={{ width: "10rem", height: "auto" }}
/>
